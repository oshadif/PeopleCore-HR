import{useState}from"react";import{useNavigate}from"react-router-dom";import{api}from"../api";export default function Login(){const[e,setE]=useState("admin@demo.com"),[p,setP]=useState("admin123"),[m,setM]=useState(""),n=useNavigate();async function s(x){x.preventDefault();try{const r=await api("/auth/login",{method:"POST",body:JSON.stringify({email:e,password:p})});localStorage.setItem("token",r.token);localStorage.setItem("user",JSON.stringify(r.user));n("/")}catch(x){setM(x.message)}}return <div className="login"><form onSubmit={s}><h1>PeopleCore HR</h1><p>Manage people, attendance, leave, and payroll.</p><label>Email<input value={e} onChange={x=>setE(x.target.value)}/></label><label>Password<input type="password" value={p} onChange={x=>setP(x.target.value)}/></label><button className="primary">Sign in</button>{m&&<p>{m}</p>}</form></div>}