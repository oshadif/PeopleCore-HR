export const API_URL=import.meta.env.VITE_API_URL||"http://localhost:4003/api";export async function api(p,o={}){const t=localStorage.getItem("token");const r=await fetch(API_URL+p,{...o,headers:{"Content-Type":"application/json",...(t?{Authorization:`Bearer ${t}`}:{}) ,...(o.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||"Request failed.");return d}export const money=n=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(Number(n||0));