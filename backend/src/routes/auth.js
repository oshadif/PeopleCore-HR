import{Router}from"express";import bcrypt from"bcryptjs";import jwt from"jsonwebtoken";import{pool}from"../db.js";const r=Router();
r.post("/login",async(req,res)=>{const q=await pool.query(`SELECT u.*,e.employee_number,e.first_name,e.last_name FROM users u LEFT JOIN employees e ON e.id=u.employee_id WHERE u.email=$1 AND u.active=true`,[req.body.email?.toLowerCase()]);const u=q.rows[0];if(!u||!(await bcrypt.compare(req.body.password||"",u.password_hash)))return res.status(401).json({message:"Invalid email or password."});const user={id:u.id,name:u.name,email:u.email,role:u.role,employeeId:u.employee_id,employeeNumber:u.employee_number};res.json({user,token:jwt.sign(user,process.env.JWT_SECRET,{expiresIn:"7d"})})});export default r;